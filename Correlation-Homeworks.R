

seed1 = sample(1000:1999,1)
seed2 = sample(2000:2999,1)

cat("\nCompute the Correlation Coefficient for the following data sets")


cat("\n\nHint: To generate X, simply type in set.seed(",seed1,"); X <- sample(100:150,10,TRUE)")
cat("\n\nHint: To generate Y, simply type in set.seed(",seed2,"); Y <- sample(100:150,10,TRUE)")
